// Phase 5 will mount the real Electron main process here.
// This stub exists so `tsc -p tsconfig.main.json` has at least one input
// and CI typecheck can pass before the main process lands.
export {};
